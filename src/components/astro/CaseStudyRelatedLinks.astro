---
import type { ProjectLink } from "../../data/projects";

interface Props {
  links: ProjectLink[];
}

const { links } = Astro.props as Props;
---

{links.length ? (
  <section class="mt-20 mb-20">
    <h3 class="text-2xl font-display font-700 text-text-primary mb-6">Related Links</h3>
    <div class="space-y-4">
      {links.map((link) => (
        <a
          href={link.href}
          target="_blank"
          rel="noopener noreferrer"
          class="block rounded-xl border border-border bg-bg-elevated/60 p-6 hover:border-accent-border transition-colors duration-300"
        >
          <div class="flex items-center justify-between gap-4">
            <div>
              <p class="text-text-primary font-display font-700">{link.title}</p>
              <p class="text-text-secondary font-body text-sm mt-1">{link.description}</p>
            </div>
            <span class="text-accent">&nearr;</span>
          </div>
        </a>
      ))}
    </div>
  </section>
) : null}
